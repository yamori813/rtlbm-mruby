name: mruby on YABM CI

on: [push]

jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./rtlbm-mruby

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - uses: actions/checkout@v5
      with:
        path: rtlbm-mruby
    - name: Checkout tools repo
      uses: actions/checkout@v4
      with:
        repository: yamori813/rtl819x-toolchain
        path: rtl819x-toolchain
    - uses: actions/cache@v4
      with:
        path: rtl819x-toolchain
        key: rtl819x-toolchain
    - name: get files
      run: cd build;./getfiles.sh
    - name: make newlib
      run: cd build;./mknewlib.sh
#    - name: make lwip
#      run: ./build/mklwip.sh
#    - name: make bearssl
#      run: ./build/mkbearssl.sh
#    - name: make mruby
#      run: ./build/mkmruby.sh
#    - name: make allvm
#      run: ./mkallvm.sh

