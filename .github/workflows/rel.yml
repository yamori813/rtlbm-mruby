name: mruby on YABM CI

on: [push]

jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - uses: actions/checkout@v5
      with:
        path: rtlbm-mruby
    - name: Checkout tools repo
      uses: actions/checkout@v4
      with:
        repository: yamori813/rtl819x-toolchain
        path: rtl819x-toolchain
    - name: get files
      run: env
#    - name: get files
#      run: cd rtlbm-mruby/build;./getfiles.sh
#    - name: make newlib
#      run: cd rtlbm-mruby/build;./mknewlib.sh
#    - name: make lwip
#      run: cd build;./mklwip.sh
#    - name: make bearssl
#      run: cd build;./mkbearssl.sh
#    - name: make mruby
#      run: cd build;./mkmruby.sh
#    - name: make allvm
#      run: ./mkallvm.sh

